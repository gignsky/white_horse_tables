mod error_tester;
mod utils_tester;
// mod import;

// #[test]
// fn run_test() {
//     let result = crate::run(true);
//     assert!(result.is_ok());
// }

#[test]
fn main_test() {
    let result = crate::main);
    assert!(result.is_ok());
}
